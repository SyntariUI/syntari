{
  "name": "orbit-ui",
  "version": "0.2.0",
  "private": true,
  "description": "Orbit UI: interactive components, source installer, and documentation",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "playwright test",
    "figma:extract": "node scripts/extract-figma.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "bin": {
    "orbit": "cli.mjs"
  },
  "files": [
    "kit",
    "cli.mjs"
  ]
}
