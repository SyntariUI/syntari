{
  "name": "orbit-ui",
  "version": "0.1.0",
  "private": true,
  "description": "A framework-independent interactive UI component gallery",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "playwright test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
