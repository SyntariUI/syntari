{
  "name": "syntari-ui",
  "version": "0.2.1",
  "private": false,
  "description": "Syntari: an agentic design system with interactive components, tokens, patterns, and a machine-readable registry.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "playwright test",
    "figma:extract": "node scripts/extract-figma.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "bin": {
    "syntari": "cli.mjs"
  },
  "files": [
    "kit",
    "cli.mjs"
  ]
}
